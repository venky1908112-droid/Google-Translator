# Google Translator (Python)

A simple Python application that translates text from one language to another using a translation library.

## Features
- Translate text between multiple languages
- Easy-to-use command-line interface
- Uses translation APIs/libraries

## Technologies Used
- Python
- Translation Library

## How to Run
1. Install required library

pip install googletrans

2. Run the program

python translator.py